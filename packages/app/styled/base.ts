import { css } from "styled-components";
import { primary, spacing } from "../styles/constants";

export const CenteredText = css`
  text-align: center;
`;

export const SubHeader = css`
  margin-bottom: ${spacing(5)}rem;
  color: ${primary};
`;
